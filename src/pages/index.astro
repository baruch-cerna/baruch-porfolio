---
import Navbar from "../components/Navbar.astro";
import Section from "../components/Section.astro";
import Layout from "../layouts/Layout.astro";
import LocationIcon from "../../public/media/location.svg?raw";
import { addAttributesToInnerHTMLString } from "../data/utils";
import { projects } from "../data/index.data";
import Card from "../components/Card.astro";
import Logo from "/public/media/logo_blanco.svg?raw";
import CTA from "../components/CTA.astro";

const locationIcon = addAttributesToInnerHTMLString(LocationIcon, {
  width: "2.5rem",
  height: "3rem",
  fill: "rgb(var(--accent-light))",
});

const logoHTML = addAttributesToInnerHTMLString(Logo, { class: "logo-svg" });
---

<Layout title="Baruch Cerna | Desarrollador Web">
  <!-- <Navbar /> -->
  <Section>
    <div class="header-container">
      <div class="row">
        <p class="location">
          <Fragment set:html={locationIcon} /> Ciudad de México
        </p>
        <a href="/" class="logo">
          <Fragment set:html={logoHTML} />
        </a>
      </div>
      <h1 class="enter">Me llamo Baruch Cerna y soy Desarrollador Web</h1>
      <CTA text="Trabajemos juntos" />
      <p>
        Tengo más de 5 años diseñando soluciones y escribiendo código que ha
        ayudado a resolver problemas para startups y empresas multinacionales.
        Me encanta enfrentar nuevos retos y me apasiona la idea de transformar
        ideas en experiencias digitales que agreguen valor a las empresas y a
        los usuarios.
      </p>
      <p><em>"La respuesta es más elegante si es simple."</em></p>
    </div>
  </Section>
  <Section>
    <h2 id="projects">Proyectos</h2>
    <p>
      La adaptabilidad y la innovación son claves para superar los retos y
      alcanzar el objetivo. A lo largo de mi experiencia, he desarrollado
      soluciones eficientes y a medida, aplicando nuevas tecnologías y enfoques
      que aseguran resultados de alto impacto en cada etapa del proceso.
    </p>
    <div class="projects">
      {
        projects.map((project) => (
          <Card
            title={project.title}
            body={project.body}
            href={project.href}
            image={project.image}
            technologies={project.technologies}
          />
        ))
      }
    </div>
    <div class="cta-container">
      <CTA text="Contactame" />
    </div>
  </Section>

  <footer>
    <p>
      Hecho con ❤️ y <a href="https://astro.build" target="_blank">Astro</a>
      por
      <a href="https://github.com/baruch-cerna" target="_blank">Baruch Cerna</a>
    </p>
  </footer>
</Layout>

<style>
  .header-container {
    display: flex;
    flex-direction: column;
    height: 100%;
    width: 100%;
    gap: 3rem;
    margin: 2rem 0;
  }

  .row {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
  }

  .logo {
    height: 100px;
    margin: 0 17px;
  }

  h1 {
    font-size: 7rem;
  }

  h2 {
    font-size: 3rem;
  }

  h3 {
    font-size: 2rem;
    font-weight: 500;
  }

  p {
    font-size: 2rem;
  }

  ul {
    padding: 0;
    display: flex;
    flex-direction: row;
    justify-content: center;
    flex-wrap: wrap;
    gap: 1rem;
    max-width: 100%;
    height: auto;
    margin-top: 3rem;
  }

  .cards {
    display: flex;
    flex-direction: column;
    flex-wrap: wrap;
    gap: 1rem;
    width: 100%;
    height: 100%;
    margin-top: 3rem;
  }

  .projects {
    display: flex;
    flex-direction: row;
    align-items: center;
    height: 100%;
    width: 100%;
    gap: 1rem;
    justify-content: space-between;
    flex-wrap: wrap;
    margin: 2rem 0;
  }

  .location {
    display: inline-flex;
    align-items: center;
    margin: 0;
  }

  .cta-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100%;
    width: 100%;
    margin: 4rem 0;
  }

  footer {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-end;
    height: 100%;
    width: 100%;
    padding: 2rem;
  }

  @media screen and (max-width: 1024px) {
    h1 {
      font-size: 6rem;
    }
  }

  @media screen and (max-width: 768px) {
    .header-container {
      gap: 1rem;
    }

    h1 {
      font-size: 5.5rem;
    }
    h2 {
      font-size: 2rem;
    }
  }

  @media screen and (max-width: 575px) {
    .header-container {
      gap: 1rem;
    }

    h1 {
      font-size: 3rem;
    }
    h2 {
      font-size: 2rem;
    }
    p {
      font-size: 1.5rem;
    }

    .row {
      flex-direction: column-reverse;
    }
  }
</style>
